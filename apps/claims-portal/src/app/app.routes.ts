import { Routes } from '@angular/router';
import { ClaimIntakeComponent } from './claims/claim-intake.component';

export const routes: Routes = [
  {
    path: '',
    component: ClaimIntakeComponent
  }
];
