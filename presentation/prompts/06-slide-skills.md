---
slide: 6
title: Skills
style: hand-drawn field-notes infographic
aspect_ratio: 16:9
sources:
  - https://code.claude.com/docs/en/skills
  - https://resources.anthropic.com/hubfs/The-Complete-Guide-to-Building-Skill-for-Claude.pdf?hsLang=en
---

Create a premium 16:9 presentation slide in the existing hand-drawn field-notes style.

Title text only at the top: "Skills"

Main visual: a hand-drawn file folder opened like a small field guide. The folder is labeled `.claude/skills/signal-forms/`. Inside it, show a central document labeled `SKILL.md` and a few optional supporting items:

- `references/`
- `examples/`
- `scripts/`

On the `SKILL.md` page, show a small readable frontmatter block at the top:

`---`
`description: Use for Angular Signal Forms`
`---`

Below the frontmatter, show a larger but mostly abstract section labeled:

`procedure`

At the bottom, draw a long horizontal context bar. Only a small chip from the frontmatter is already inside the bar, labeled:

`description`

Add a tiny label above that chip:

`catalog`

Then draw a light dotted arrow from the lower `procedure` section of `SKILL.md`, plus the nearby `references/`, `examples/`, and `scripts/` folders, into a later empty space in the context bar. Label that arrow:

`loaded when chosen`

Near the folder, include a tiny slash-command tag:

`/signal-forms`

Include the recurring orange pixel-art Claude blob mascot reading the folder. The blob has no mouth, black square eyes, two stubby side arms, and four short legs.

The visual should communicate: a skill is a file-backed procedure; the description/frontmatter is the small catalog entry available up front; the model decides whether to use it unless the user invokes it directly by name; and supporting files/scripts/references can sit beside the main skill.

Composition: a little more informational than the chapter cards, but still airy, sparse, and drawable. Keep the readable text short.

Style: warm off-white paper background, hand-drawn ink lines, restrained orange accent, light natural shadows, modern editorial polish, legible title.

Constraints: no paragraphs, no bullet lists visible in the image, no large code block, no vendor logos, no dense technical labels. Keep all code-like text short and exactly spelled. Do not add extra explanatory text beyond the labels above.
